// Temporary frontend-only mock credentials. Replace with backend JWT auth before production launch.
export const TEMPORARY_MOCK_ADMIN_CREDENTIALS = {
  email: 'admin@omproperties.com',
  password: 'Admin@123',
}
